"""Driftgram desktop-app layer: process lifecycle, threading, OS integration.

Everything here is about running the sync engine as an application rather
than as a script. It knows nothing about Qt - the GUI sits on top of it.
"""
